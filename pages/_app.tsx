import "../styles/globals.css";
import type { AppProps } from "next/app";
import { QueryClientProvider, QueryClient } from "@tanstack/react-query";
import { SessionProvider } from "next-auth/react";
import Head from "next/head";
import UserInfo from "components/UserInfo";

export default function App({
  Component,
  pageProps: { session, ...pageProps },
}: AppProps) {
  const queryClient = new QueryClient({
    defaultOptions: {
      queries: { staleTime: Infinity },
    },
  });

  return (
    <SessionProvider session={session}>
      <QueryClientProvider client={queryClient}>
        <div className="relative mt-5 mb-2 sm:mb-3 mx-10 xl:mx-20 2xl:mx-32">
          <Head>
            <title>Belisy Commerce</title>
            <meta charSet="utf-8" />
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/favicon.ico" />

            <meta
              property="og:url"
              content="https://belisy-commerce.vercel.app"
            />
            <meta property="og:type" content="website" />
            <meta property="og:title" content="Belisy Commerce" />
            <meta
              property="og:description"
              content="Belisy Commerce의 다양한 상품을 확인해보세요."
            />
          </Head>

          <div className="mx-auto text-center mb-5 text-2xl sm:text-5xl md:text-7xl font-bold text-pink-500">
            Belisy Commerce
          </div>

          <div className="relative right-0 mb-2 text-xs sm:text-base md:text-lg lg:text-xl">
            <UserInfo />
          </div>

          <Component {...pageProps} />

          <footer className="mt-5 sm:mt-8 md:mt-10 text-center">
            <div className="border mb-3"></div>
            <div>문의: lssoy77@gmail.com</div>
            <a
              href="https://github.com/Belisy/belisy-commerce"
              target="_blank"
              rel="noopener noreferrer"
            >
              <span>💕</span>
            </a>
          </footer>
        </div>
      </QueryClientProvider>
    </SessionProvider>
  );
}
